# Entrada do exemplo

`rsm_input.json` contém `B`, `mse` e `(DᵀD)⁻¹` reconstruídos pelo mesmo trecho do notebook 03 para `m4_low`, seed 103. As funções verdadeiras foram usadas uma única vez na preparação desta fixture externa ao pacote; o CNBI recebe apenas o RSM e sua incerteza.

Proveniência detalhada: `tests/fixtures/PROVENANCE.json`.
